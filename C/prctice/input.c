#include <stdio.h>
int main()
{
    int x;
    printf("Enter the integer value: "); // Here is the line where we write the number
    scanf("%d", &x); //This is the line for write the number after run the program
    printf("The number is: %d",x);
    return 0;
}